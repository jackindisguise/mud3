{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Gamestate",
	"type": "object",
	"required": ["elapsedTime", "lastSaved", "nextCharacterId", "nextObjectId"],
	"properties": {
		"elapsedTime": {
			"type": "integer",
			"minimum": 0,
			"description": "The amount of time elapsed over all instances of the game."
		},
		"lastSaved": {
			"type": "string",
			"format": "date-time",
			"description": "Last time we saved the gamestate."
		},
		"nextCharacterId": {
			"type": "integer",
			"minimum": 1,
			"description": "The next character ID that will be assigned."
		},
		"nextObjectId": {
			"type": "integer",
			"minimum": 1,
			"description": "The next object ID that will be assigned."
		}
	},
	"additionalProperties": false
}
